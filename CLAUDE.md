# Strategy-Game (mark3-mobile)

문명풍 육각 전략 게임. 문명의 결정론적 전투와 "정복 아니면 무시" 이분법을 고치려는
시도다. 지금은 웹(Expo + React Native Web)으로 돌고, PC 출시를 목표로 한다.

## 클라우드와 PC 사이 — 다리는 git 이다

클라우드 세션은 격리된 샌드박스에서 돈다. GitHub 에서 clone 해서 작업하고 브랜치를
push 할 수 있을 뿐, 이 PC 의 디스크를 보거나 PC 에서 명령을 돌릴 수 없다.

    클라우드에서 작업 → 브랜치 push → PC 에서 pull → PC 가 주 사본, 실행도 PC

**클라우드 세션이 지킬 것**

- `main` 에 직접 push 하지 않는다. `cloud/<하려는일>` 이름으로 브랜치를 만들어 push 한다.
- push 전에 반드시 `npx tsc --noEmit` 가 깨끗해야 한다.
- 규칙(엔진·경제·AI 가중치)을 건드렸으면 `npm run sim:eval` 을 11 과 21 **양쪽** 에서
  돌리고 결과를 커밋 메시지에 적는다. 한쪽만 재고 일반화하면 반드시 틀린다 —
  이 저장소가 그걸로 여러 번 데었다.
- **화면은 클라우드에서 확인할 수 없다.** 브라우저로 눌러보는 검증은 PC 에서만
  가능하다. UI 를 고쳤으면 "확인했다" 고 쓰지 말고 "PC 에서 눌러봐야 한다" 고 남긴다.

**PC 쪽에서 받는 법**

    git fetch --all --prune
    git branch -r --sort=-committerdate | head        # 뭐가 왔나
    git merge --ff-only origin/cloud/<이름>           # 또는 checkout 해서 확인

## 구조

    mark3-mobile/src/engine/    규칙과 AI. React 를 모른다 — 그래서 화면 없이
                                수천 판을 돌릴 수 있고, 난수는 전부 주입받는다.
    mark3-mobile/src/screens/   화면. GameScreen.tsx 가 거의 전부다.
    mark3-mobile/src/services/  전투 계산, 저장소 접근
    mark3-mobile/sim/           하네스. 게임이 아니라 게임을 재는 도구다.
    mark3-mobile/docs/          측정 기록 — 무엇을 재고 왜 버렸는지

`src/engine` 이 React 를 모르는 것이 이 저장소의 핵심 성질이다. 3D 도 RL 도
하네스도 전부 그 덕에 싸게 붙었다. 깨뜨리지 말 것.

## 하네스 — 판정이지 학습이 아니다

    npm run sim:eval            이 게임 괜찮은가 — 6가지 판정 (기본 11x11)
    npm run sim:eval -- --size 21 --games 260
    npm run sim:boards          판 크기를 바꾸면 게임이 어떻게 달라지나
    npm run sim:seats           자리(차례 순서)가 공정한가 — A/A 검사
    npm run sim:victory         사람들이 무엇으로 이기나
    npm run sim:obey            속국이 명령을 실제로 따르나
    npm run sim:levers          경제 상수 하나를 돌리면 뭐가 달라지나
    npm run sim:planner         작전 탐색이 정말 나은가 (대조군 포함)
    npm run sim:diplo           외교·행군 사건이 실제로 얼마나 일어나나 (켜고 끄고 견주기)

규칙을 건드릴 때마다 도는 것은 판정(분 단위)이다. 학습(`sim:learn`, 시간 단위)은
하네스가 깨졌을 때만 돌린다. 지금 11x11 은 6/6, 21x21 은 6/6 이지만 여유가 얇다 —
최고 성격이 33.5% (문턱 35%), 시드 하나만 떼면 4/6 도 나온다. 외교 이전 main 도
21x21 은 5/6(수비형 37.5%)이었다. docs/diplomacy-2026-09-26.md 참고.

## 이 저장소에서 여러 번 덴 것들

- **한 조건에서 재고 일반화하지 않는다.** 가중치 하나를 혼자 바꿔 좋아 보였는데
  다섯이 모두 그러니 아무도 안 싸웠고, 같은 시드로 세 번 재고 "진짜" 라고 할 뻔했다.
  시드를 바꿔 다시 재는 것까지가 측정이다.
- **점추정만 보고 채택하지 않는다.** 윌슨 구간을 같이 본다. 120판의 36.4% 가
  260판에서 33.7% 였다.
- **측정이 0 을 내놓으면 손잡이가 없는 것인지 배선이 끊긴 것인지부터 본다.**
  실제로 `checkBlocVictory` 가 eco 를 안 받아서 "효과 없음" 으로 보인 적이 있다.
- **절대값을 기준으로 박지 않는다.** 거리·행정비·승리 문턱이 칸수로 박혀 있어
  판이 커지면 뜻이 달라졌다. 지금은 셋 다 판 크기로 환산한다(boardScale,
  adminCost, dominanceShare). 11x11 에서 1 이 되도록 맞춰 작은 판은 안 바뀐다.
- **실패한 값도 남긴다.** 안 남기면 반년 뒤에 또 한다. docs/ 와 주석에 숫자째로 적는다.

## 규칙 도장

`src/engine/stamp.ts` 가 수를 고르는 데 영향을 주는 상수들을 해시한다. 가중치가
어느 규칙에서 나왔는지 `LEARNED_PROVENANCE` 에 적혀 있고, `sim:eval` 이 매번 견준다.
도장이 어긋났다고 바로 다시 학습할 일은 아니다 — 하네스가 깨졌을 때가 신호다.

## 사람이 둔 판의 기록

게임을 두면 매 턴이 브라우저에 쌓이고(`mark3.matches.v1`), 시작 화면의
"기록 내보내기" 로 파일이 나온다. 이 게임에 대해 아는 것이 거의 전부 AI 끼리
돌린 결과이므로, 사람이 둔 기록이 들어오면 그게 하네스보다 값지다.
형식은 `src/engine/matchLog.ts`, `sim/recorder.ts` 와 같은 모양이다.

## 글은 한국어로

주석·커밋 메시지·문서 전부 한국어다. 무엇을 했는지보다 **왜 그렇게 했는지와
무엇을 재봤는지** 를 쓴다. 버린 선택지도 숫자와 함께 남긴다.
